---
services:
    aws.greengrass.clientdevices.IpDetector:
        configuration:
            PORT: '8884'
    main:
        lifecycle:
            install:
                all: echo All installed
        dependencies:
            - aws.greengrass.clientdevices.IpDetector
